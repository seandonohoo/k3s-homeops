---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tempdownloads
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 250Gi
  storageClassName: openebs-hostpath
